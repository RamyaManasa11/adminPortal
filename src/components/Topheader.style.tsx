import { Box } from '@mui/material';
import { styled } from '@mui/styles';

export const LoyaltyListingHeaderContainer = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  flexDirection: 'row',
  margin: 15,
});